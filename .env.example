# Example environment variables for Pyramid Tools
# Copy this file to .env.local and adjust values as needed

# Port for the Next.js application
# Default: 3060 (production), 3050 (development)
PORT=3060

# Node environment
# Options: production, development, test
NODE_ENV=production

# OpenAI API Key for GPT-4o vision features
# Get your API key from: https://platform.openai.com/api-keys
# Required for: Handwriting to Text OCR tool
OPENAI_API_KEY=your_api_key_here

# Note: Most Pyramid Tools features are client-side and don't require environment variables.
# Server-side features (like Handwriting OCR) require the appropriate API keys above.
